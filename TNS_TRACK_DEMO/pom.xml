<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.2</version>
        <relativePath/>
    </parent>

    <groupId>cl.thenextsecurity.tns</groupId>
    <artifactId>TNS_TRACK_DEMO</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>TNS Track Backend</name>
    <description>Backend Spring Boot para TNS Track - Monitoreo camaras frias y rastreo GPS</description>

    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.6</jjwt.version>
        <spring-ai.version>1.0.0-M6</spring-ai.version>
        <!-- Centraliza version de BouncyCastle: bcprov y bcpkix deben ser identicas -->
        <bouncycastle.version>1.80</bouncycastle.version>
        <!-- Override justificado: mysql-connector-j 9.2.0 incluye fix para                -->
        <!-- TIME_ZONE handling con MySQL 8.x; BOM 3.4.2 gestiona 9.1.0                    -->
        <mysql.version>9.2.0</mysql.version>
        <!-- Override justificado: argon2-jvm 2.12 mejora compatibilidad con Java 21 LTS   -->
        <argon2-jvm.version>2.12</argon2-jvm.version>
    </properties>

    <!-- ================================================ -->
    <!-- Override de dependencias transitivas vulnerables  -->
    <!-- Fuerza versiones seguras sin cambiar la API       -->
    <!-- de las librerias que las incluyen (web-push)      -->
    <!-- ================================================ -->
    <dependencyManagement>
        <dependencies>

            <!-- Netty BOM: cubre TODOS los modulos de Netty en una sola declaracion.       -->
            <!-- Reemplaza el override modulo-a-modulo anterior que dejaba 16+ modulos      -->
            <!-- en 4.1.117.Final (con CVE-2025-58056/57, CVE-2025-67735,                  -->
            <!-- CVE-2025-25193, CVE-2025-24970).                                           -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>4.1.119.Final</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- async-http-client: CVE-2024-53990 en 2.10.4 -->
            <dependency>
                <groupId>org.asynchttpclient</groupId>
                <artifactId>async-http-client</artifactId>
                <version>2.12.3</version>
            </dependency>

            <!-- jose4j: multiples CVEs en 0.7.0 -->
            <dependency>
                <groupId>org.bitbucket.b_c</groupId>
                <artifactId>jose4j</artifactId>
                <version>0.9.6</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- ====================================== -->
        <!-- Spring Boot Core                       -->
        <!-- ====================================== -->

        <!-- REST API + Spring MVC (reemplaza Express.js) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Seguridad y autenticacion (reemplaza express-jwt + cors middleware) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Validacion de DTOs con anotaciones (@NotNull, @Email, etc.) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- WebClient para llamadas HTTP salientes (Ubibot API, Shelly API).               -->
        <!-- NOTA: spring-boot-starter-web ya activa el stack Servlet/MVC.                  -->
        <!-- WebFlux se incluye exclusivamente para usar WebClient como cliente HTTP;        -->
        <!-- Spring Boot auto-configura MVC (no WebFlux) cuando ambos estan en classpath.   -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- Hot reload en desarrollo -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- ====================================== -->
        <!-- Base de Datos                          -->
        <!-- ====================================== -->

        <!-- JPA + Hibernate + HikariCP pool de conexiones (reemplaza pool manual MySQL2).  -->
        <!-- Incluye transitivamente spring-boot-starter-jdbc; no declarar ambos.           -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Driver MySQL (reemplaza mysql2 npm).                                            -->
        <!-- Version gestionada via <mysql.version> en <properties>.                        -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ====================================== -->
        <!-- JWT - Autenticacion                    -->
        <!-- Reemplaza jsonwebtoken npm             -->
        <!-- ====================================== -->

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- ====================================== -->
        <!-- Password Hashing                       -->
        <!-- BCrypt: incluido en spring-security    -->
        <!-- Argon2: usuarios legacy con hash       -->
        <!--         argon2 en BD existente         -->
        <!-- ====================================== -->

        <dependency>
            <groupId>de.mkammerer</groupId>
            <artifactId>argon2-jvm</artifactId>
            <version>${argon2-jvm.version}</version>
        </dependency>

        <!-- ====================================== -->
        <!-- Email                                  -->
        <!-- Reemplaza @sendgrid/mail npm           -->
        <!-- ====================================== -->

        <!-- Version gestionada por Spring Boot BOM (4.10.3 en Boot 3.4.2) -->
        <dependency>
            <groupId>com.sendgrid</groupId>
            <artifactId>sendgrid-java</artifactId>
        </dependency>

        <!-- ====================================== -->
        <!-- Push Notifications PWA                 -->
        <!-- Reemplaza web-push npm                 -->
        <!-- IMPORTANTE: NO regenerar VAPID keys    -->
        <!--             al migrar (rompe           -->
        <!--             suscriptores existentes)   -->
        <!-- ====================================== -->

        <dependency>
            <groupId>nl.martijndwars</groupId>
            <artifactId>web-push</artifactId>
            <version>5.1.2</version>
        </dependency>

        <!-- BouncyCastle Provider: requerido por web-push para crypto ECDH (Java 17+/21+) -->
        <!-- Version sincronizada con bcpkix via ${bouncycastle.version}                    -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>

        <!-- BouncyCastle PKIX: lectura nativa de certificados PEM (AWS IoT Core)           -->
        <!-- Version sincronizada con bcprov via ${bouncycastle.version}                    -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>

        <!-- ====================================== -->
        <!-- Generacion de PDF                      -->
        <!-- Reemplaza PDFKit + Puppeteer           -->
        <!-- OpenHTML2PDF: HTML a PDF sin Chrome    -->
        <!-- ADVERTENCIA: openhtmltopdf sin         -->
        <!--   mantenimiento desde Sep 2021.        -->
        <!--   Evaluar migracion a Flying Saucer    -->
        <!--   (org.xhtmlrenderer:flying-saucer-pdf)-->
        <!--   o fork activo en JitPack.            -->
        <!-- ====================================== -->

        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-pdfbox</artifactId>
            <version>1.0.10</version>
        </dependency>

        <!-- Soporte SVG en PDF (graficos de reportes) -->
        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-svg-support</artifactId>
            <version>1.0.10</version>
        </dependency>

        <!-- ====================================== -->
        <!-- Spring AI - Integracion OpenAI         -->
        <!-- Reemplaza openai npm                   -->
        <!-- ADVERTENCIA: 1.0.0-M6 es milestone;   -->
        <!--   API puede cambiar sin deprecacion.   -->
        <!--   Actualizar a GA cuando este          -->
        <!--   disponible en Maven Central.         -->
        <!-- ====================================== -->

        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
            <version>${spring-ai.version}</version>
        </dependency>

        <!-- ====================================== -->
        <!-- Scheduler - Jobs programados           -->
        <!-- Reemplaza node-cron + node-schedule    -->
        <!-- metricsAggregationJob + reportCleanup  -->
        <!-- ====================================== -->

        <!-- spring-boot-starter-quartz incluye quartz + spring-context-support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-quartz</artifactId>
        </dependency>

        <!-- ====================================== -->
        <!-- MQTT                                   -->
        <!-- ====================================== -->

        <dependency>
            <groupId>org.eclipse.paho</groupId>
            <artifactId>org.eclipse.paho.client.mqttv3</artifactId>
            <version>1.2.5</version>
        </dependency>

        <!-- ====================================== -->
        <!-- Templates server-side                  -->
        <!-- Usado para renderizar HTML que luego   -->
        <!-- se convierte a PDF con openhtmltopdf   -->
        <!-- ====================================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- ====================================== -->
        <!-- Utilidades                             -->
        <!-- ====================================== -->

        <!-- Reduccion de boilerplate (getters, setters, builders) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Serializacion correcta de LocalDate/LocalDateTime en JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Declarado explicitamente porque SseService y MqttService lo usan directamente. -->
        <!-- Sin version: Spring Boot BOM gestiona la version compatible.                   -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- ====================================== -->
        <!-- Testing                                -->
        <!-- ====================================== -->

        <!-- JUnit 5, AssertJ, Mockito, Spring Test, @WebMvcTest, @DataJpaTest,             -->
        <!-- @AutoConfigureTestDatabase: todo incluido en este starter en Boot 3.x          -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- MockMvc con seguridad (para probar endpoints protegidos) -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- H2 en memoria para @DataJpaTest sin requerir MySQL en el entorno de CI -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Suite runner: junit-platform-suite-engine para que @Suite funcione como ejecutor -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers: MySQL real en Docker para tests de integracion -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <!-- Compiler plugin con soporte Lombok.                                         -->
            <!-- ${lombok.version}: propiedad gestionada por Spring Boot BOM;               -->
            <!-- necesaria en annotationProcessorPaths porque maven-compiler-plugin         -->
            <!-- no hereda versiones del BOM en esta seccion en todas sus versiones.        -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Spring Boot Maven plugin (excluye Lombok del JAR final) -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- Spring AI milestones (1.0.0-M6 aun no esta en Maven Central) -->
    <!-- JitPack para dependencias publicadas desde GitHub              -->
    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>jitpack.io</id>
            <name>JitPack</name>
            <url>https://jitpack.io</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
