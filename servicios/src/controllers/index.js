const deviceController = require('./device_Controller');
const energyController = require('./energy_Controller');
const groupController = require('./group_Controller');
const systemController = require('./system_Controller');

module.exports = {
    deviceController,
    energyController,
    groupController,
    systemController
};